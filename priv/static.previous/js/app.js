const status=document.getElementById("auth-status");async function loadSession(){try{const response=await fetch("/api/auth/me");if(response.status===401){status.textContent="Aucune session authentifiée.";return}if(!response.ok)throw new Error(`HTTP ${response.status}`);const session=await response.json();status.textContent=`Session active : ${session.user}`}catch(error){status.textContent="Le service d’authentification est indisponible.";console.error(error)}}loadSession();
