const form=document.getElementById("login-form");const status=document.getElementById("auth-status");const submit=form.querySelector("button");form.addEventListener("submit",async event=>{event.preventDefault();status.classList.remove("success");status.textContent="";submit.disabled=true;try{const response=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form)))});if(!response.ok)throw new Error("invalid_credentials");const session=await response.json();sessionStorage.setItem("game-simulator-token",session.token);status.classList.add("success");status.textContent=`Session ouverte pour ${session.user}.`}catch(_error){status.textContent="Utilisateur ou mot de passe incorrect."}finally{submit.disabled=false}});
